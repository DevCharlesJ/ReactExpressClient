module.exports = {
    
    SERVERS:  // Note: React runs on 3000 by default
    {
        // Connection to the server that handles client requests for ChatApp
        ChatApp: 'http://localhost:3001'
    }
}